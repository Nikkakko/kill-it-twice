{
  "name": "@kill-it-twice/worker",
  "private": true,
  "scripts": { "build": "tsc -p tsconfig.json", "dev": "tsx watch src/main.ts", "lint": "tsc -p tsconfig.json --noEmit", "test": "jest" },
  "dependencies": {
    "@elastic/elasticsearch": "^8.15.0",
    "@kill-it-twice/contracts": "workspace:*",
    "@kill-it-twice/database": "workspace:*",
    "@nestjs/common": "^11.0.11",
    "@nestjs/core": "^11.0.11",
    "amqplib": "^0.10.5",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": { "tsx": "^4.19.2", "@types/amqplib": "^0.10.5", "@types/node": "^22.10.2" }
}
